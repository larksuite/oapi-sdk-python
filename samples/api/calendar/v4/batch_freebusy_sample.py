# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.calendar.v4 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: BatchFreebusyRequest = BatchFreebusyRequest.builder() \
        .user_id_type("user_id") \
        .request_body(BatchFreebusyRequestBody.builder()
                      .time_min("2020-10-28T12:00:00+08:00")
                      .time_max("2020-10-28T12:00:00+08:00")
                      .user_ids([])
                      .include_external_calendar(True)
                      .only_busy(True)
                      .need_rsvp_status(True)
                      .build()) \
        .build()

    # 发起请求
    response: BatchFreebusyResponse = client.calendar.v4.freebusy.batch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.calendar.v4.freebusy.batch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: BatchFreebusyRequest = BatchFreebusyRequest.builder() \
        .user_id_type("user_id") \
        .request_body(BatchFreebusyRequestBody.builder()
                      .time_min("2020-10-28T12:00:00+08:00")
                      .time_max("2020-10-28T12:00:00+08:00")
                      .user_ids([])
                      .include_external_calendar(True)
                      .only_busy(True)
                      .need_rsvp_status(True)
                      .build()) \
        .build()

    # 发起请求
    response: BatchFreebusyResponse = await client.calendar.v4.freebusy.abatch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.calendar.v4.freebusy.abatch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.cardkit.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateCardRequest = UpdateCardRequest.builder() \
        .card_id("7355372766134157313") \
        .request_body(UpdateCardRequestBody.builder()
                      .card(Card.builder().build())
                      .uuid("191857678434")
                      .sequence(1712578784)
                      .build()) \
        .build()

    # 发起请求
    response: UpdateCardResponse = client.cardkit.v1.card.update(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.cardkit.v1.card.update failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateCardRequest = UpdateCardRequest.builder() \
        .card_id("7355372766134157313") \
        .request_body(UpdateCardRequestBody.builder()
                      .card(Card.builder().build())
                      .uuid("191857678434")
                      .sequence(1712578784)
                      .build()) \
        .build()

    # 发起请求
    response: UpdateCardResponse = await client.cardkit.v1.card.aupdate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.cardkit.v1.card.aupdate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

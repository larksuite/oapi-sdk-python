# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.wiki.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SearchNodeRequest = SearchNodeRequest.builder() \
        .page_token("str") \
        .page_size(20) \
        .request_body(SearchNodeRequestBody.builder()
                      .query("123")
                      .space_id("6967549310762221587")
                      .node_id("O5UIw9NA8iW308kQQeybWLZqcXc")
                      .build()) \
        .build()

    # 发起请求
    response: SearchNodeResponse = client.wiki.v1.node.search(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.wiki.v1.node.search failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SearchNodeRequest = SearchNodeRequest.builder() \
        .page_token("str") \
        .page_size(20) \
        .request_body(SearchNodeRequestBody.builder()
                      .query("123")
                      .space_id("6967549310762221587")
                      .node_id("O5UIw9NA8iW308kQQeybWLZqcXc")
                      .build()) \
        .build()

    # 发起请求
    response: SearchNodeResponse = await client.wiki.v1.node.asearch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.wiki.v1.node.asearch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.minutes.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SubscriptionMinuteRequest = SubscriptionMinuteRequest.builder() \
        .request_body(SubscriptionMinuteRequestBody.builder()
                      .event_type("minutes.minute.generated_v1")
                      .build()) \
        .build()

    # 发起请求
    response: SubscriptionMinuteResponse = client.minutes.v1.minute.subscription(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.minutes.v1.minute.subscription failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SubscriptionMinuteRequest = SubscriptionMinuteRequest.builder() \
        .request_body(SubscriptionMinuteRequestBody.builder()
                      .event_type("minutes.minute.generated_v1")
                      .build()) \
        .build()

    # 发起请求
    response: SubscriptionMinuteResponse = await client.minutes.v1.minute.asubscription(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.minutes.v1.minute.asubscription failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

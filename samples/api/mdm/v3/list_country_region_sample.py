# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.mdm.v3 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListCountryRegionRequest = ListCountryRegionRequest.builder() \
        .languages([]) \
        .fields([]) \
        .limit(10) \
        .offset(0) \
        .return_count(bool) \
        .page_token("str") \
        .request_body(ListCountryRegionRequestBody.builder()
                      .filter(Filter.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: ListCountryRegionResponse = client.mdm.v3.country_region.list(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.mdm.v3.country_region.list failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListCountryRegionRequest = ListCountryRegionRequest.builder() \
        .languages([]) \
        .fields([]) \
        .limit(10) \
        .offset(0) \
        .return_count(bool) \
        .page_token("str") \
        .request_body(ListCountryRegionRequestBody.builder()
                      .filter(Filter.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: ListCountryRegionResponse = await client.mdm.v3.country_region.alist(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.mdm.v3.country_region.alist failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

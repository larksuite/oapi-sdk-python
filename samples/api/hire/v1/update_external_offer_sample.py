# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.hire.v1 import *


def main():
	# 创建client
	client = lark.Client.builder() \
		.app_id(lark.APP_ID) \
		.app_secret(lark.APP_SECRET) \
		.log_level(lark.LogLevel.DEBUG) \
		.build()

	# 构造请求对象
	request: UpdateExternalOfferRequest = UpdateExternalOfferRequest.builder() \
		.external_offer_id("6960663240925956660") \
		.request_body(ExternalOffer.builder()
					  .external_application_id("7395015673275697419")
					  .biz_create_time("1721899352428")
					  .owner("张三")
					  .offer_status("已发送")
					  .attachment_id_list([])
					  .build()) \
		.build()

	# 发起请求
	response: UpdateExternalOfferResponse = client.hire.v1.external_offer.update(request)

	# 处理失败返回
	if not response.success():
		lark.logger.error(
			f"client.hire.v1.external_offer.update failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
		return

	# 处理业务结果
	lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
	# 创建client
	client = lark.Client.builder() \
		.app_id(lark.APP_ID) \
		.app_secret(lark.APP_SECRET) \
		.log_level(lark.LogLevel.DEBUG) \
		.build()

	# 构造请求对象
	request: UpdateExternalOfferRequest = UpdateExternalOfferRequest.builder() \
		.external_offer_id("6960663240925956660") \
		.request_body(ExternalOffer.builder()
					  .external_application_id("7395015673275697419")
					  .biz_create_time("1721899352428")
					  .owner("张三")
					  .offer_status("已发送")
					  .attachment_id_list([])
					  .build()) \
		.build()

	# 发起请求
	response: UpdateExternalOfferResponse = await client.hire.v1.external_offer.aupdate(request)

	# 处理失败返回
	if not response.success():
		lark.logger.error(
			f"client.hire.v1.external_offer.aupdate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
		return

	# 处理业务结果
	lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
	# asyncio.run(amain()) 异步方式
	main()

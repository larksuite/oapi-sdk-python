# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.spark.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UploadInitializeAppStorageRequest = UploadInitializeAppStorageRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .request_body(UploadInitializeAppStorageRequestBody.builder()
                      .file_name("上传文件名称示例")
                      .file_size(104857600)
                      .mime_type("text/plain; charset=utf-8")
                      .build()) \
        .build()

    # 发起请求
    response: UploadInitializeAppStorageResponse = client.spark.v1.app_storage.upload_initialize(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_storage.upload_initialize failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UploadInitializeAppStorageRequest = UploadInitializeAppStorageRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .request_body(UploadInitializeAppStorageRequestBody.builder()
                      .file_name("上传文件名称示例")
                      .file_size(104857600)
                      .mime_type("text/plain; charset=utf-8")
                      .build()) \
        .build()

    # 发起请求
    response: UploadInitializeAppStorageResponse = await client.spark.v1.app_storage.aupload_initialize(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_storage.aupload_initialize failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.spark.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: CreateAppRequest = CreateAppRequest.builder() \
        .request_body(CreateAppRequestBody.builder()
                      .name("智能客服助手")
                      .app_type("str")
                      .description("提供7×24小时智能对话服务，支持常见问题自动解答与工单流转")
                      .icon_url("https://example.com/app-icons/customer-service.png")
                      .build()) \
        .build()

    # 发起请求
    response: CreateAppResponse = client.spark.v1.app.create(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app.create failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: CreateAppRequest = CreateAppRequest.builder() \
        .request_body(CreateAppRequestBody.builder()
                      .name("智能客服助手")
                      .app_type("str")
                      .description("提供7×24小时智能对话服务，支持常见问题自动解答与工单流转")
                      .icon_url("https://example.com/app-icons/customer-service.png")
                      .build()) \
        .build()

    # 发起请求
    response: CreateAppResponse = await client.spark.v1.app.acreate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app.acreate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

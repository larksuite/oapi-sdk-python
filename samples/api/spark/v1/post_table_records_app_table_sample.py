# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.spark.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PostTableRecordsAppTableRequest = PostTableRecordsAppTableRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .table_name("table_name_1") \
        .columns("name,age") \
        .on_conflict("user_id,product_id") \
        .env("online") \
        .user_identifier_type("miaoda_user_id") \
        .request_body(PostTableRecordsAppTableRequestBody.builder()
                      .records("")
                      .build()) \
        .build()

    # 发起请求
    response: PostTableRecordsAppTableResponse = client.spark.v1.app_table.post_table_records(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_table.post_table_records failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PostTableRecordsAppTableRequest = PostTableRecordsAppTableRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .table_name("table_name_1") \
        .columns("name,age") \
        .on_conflict("user_id,product_id") \
        .env("online") \
        .user_identifier_type("miaoda_user_id") \
        .request_body(PostTableRecordsAppTableRequestBody.builder()
                      .records("")
                      .build()) \
        .build()

    # 发起请求
    response: PostTableRecordsAppTableResponse = await client.spark.v1.app_table.apost_table_records(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_table.apost_table_records failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.spark.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateAppVisibilityAppRequest = UpdateAppVisibilityAppRequest.builder() \
        .app_id("app-20240520103000-xyz") \
        .user_id_type("user_id") \
        .request_body(UpdateAppVisibilityAppRequestBody.builder()
                      .users([])
                      .departments([])
                      .chats([])
                      .apply_config(ApplyConfig.builder().build())
                      .require_login(bool)
                      .scope("str")
                      .build()) \
        .build()

    # 发起请求
    response: UpdateAppVisibilityAppResponse = client.spark.v1.app.update_app_visibility(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app.update_app_visibility failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateAppVisibilityAppRequest = UpdateAppVisibilityAppRequest.builder() \
        .app_id("app-20240520103000-xyz") \
        .user_id_type("user_id") \
        .request_body(UpdateAppVisibilityAppRequestBody.builder()
                      .users([])
                      .departments([])
                      .chats([])
                      .apply_config(ApplyConfig.builder().build())
                      .require_login(bool)
                      .scope("str")
                      .build()) \
        .build()

    # 发起请求
    response: UpdateAppVisibilityAppResponse = await client.spark.v1.app.aupdate_app_visibility(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app.aupdate_app_visibility failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.spark.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    file = open("file_path", "rb")
    request: DownloadAppStorageRequest = DownloadAppStorageRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .file_key("1859988692091946") \
        .file_url("/app/app_4jmwuym484u90/runtime/api/v1/storage/object/bucket_aadju74v6daba/1859988692091946") \
        .build()

    # 发起请求
    response: DownloadAppStorageResponse = client.spark.v1.app_storage.download(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_storage.download failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    f = open(f"/file_path/{response.file_name}", "wb")
    f.write(response.file.read())
    f.close()


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    file = open("file_path", "rb")
    request: DownloadAppStorageRequest = DownloadAppStorageRequest.builder() \
        .app_id("app_4jcn5n11bpf5v") \
        .file_key("1859988692091946") \
        .file_url("/app/app_4jmwuym484u90/runtime/api/v1/storage/object/bucket_aadju74v6daba/1859988692091946") \
        .build()

    # 发起请求
    response: DownloadAppStorageResponse = await client.spark.v1.app_storage.adownload(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.spark.v1.app_storage.adownload failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    f = open(f"/file_path/{response.file_name}", "wb")
    f.write(response.file.read())
    f.close()


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

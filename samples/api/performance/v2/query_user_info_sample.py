# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.performance.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: QueryUserInfoRequest = QueryUserInfoRequest.builder() \
        .user_id_type("user_id") \
        .department_id_type("open_department_id") \
        .request_body(QueryUserInfoRequestBody.builder()
                      .semester_id("6992035450862224940")
                      .user_ids([])
                      .build()) \
        .build()

    # 发起请求
    response: QueryUserInfoResponse = client.performance.v2.user_info.query(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.performance.v2.user_info.query failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: QueryUserInfoRequest = QueryUserInfoRequest.builder() \
        .user_id_type("user_id") \
        .department_id_type("open_department_id") \
        .request_body(QueryUserInfoRequestBody.builder()
                      .semester_id("6992035450862224940")
                      .user_ids([])
                      .build()) \
        .build()

    # 发起请求
    response: QueryUserInfoResponse = await client.performance.v2.user_info.aquery(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.performance.v2.user_info.aquery failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

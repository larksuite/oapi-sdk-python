# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.mail.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PatchUserMailboxMailContactRequest = PatchUserMailboxMailContactRequest.builder() \
        .user_mailbox_id("user@xxx.xx 或 me") \
        .mail_contact_id("123") \
        .request_body(MailContact.builder()
                      .name("张三")
                      .company("张三科技有限公司")
                      .phone("19912341234")
                      .mail_address("zhangsan@example.com")
                      .tag("朋友")
                      .remark("飞书发布会认识")
                      .position("CFO")
                      .build()) \
        .build()

    # 发起请求
    response: PatchUserMailboxMailContactResponse = client.mail.v1.user_mailbox_mail_contact.patch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.mail.v1.user_mailbox_mail_contact.patch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PatchUserMailboxMailContactRequest = PatchUserMailboxMailContactRequest.builder() \
        .user_mailbox_id("user@xxx.xx 或 me") \
        .mail_contact_id("123") \
        .request_body(MailContact.builder()
                      .name("张三")
                      .company("张三科技有限公司")
                      .phone("19912341234")
                      .mail_address("zhangsan@example.com")
                      .tag("朋友")
                      .remark("飞书发布会认识")
                      .position("CFO")
                      .build()) \
        .build()

    # 发起请求
    response: PatchUserMailboxMailContactResponse = await client.mail.v1.user_mailbox_mail_contact.apatch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.mail.v1.user_mailbox_mail_contact.apatch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.application.v7 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PatchApplicationConfigRequest = PatchApplicationConfigRequest.builder() \
        .app_id("cli_a306c5476fb8d00c") \
        .department_id_type("open_department_id") \
        .user_id_type("open_id") \
        .request_body(PatchApplicationConfigRequestBody.builder()
                      .scope(AppConfigScope.builder().build())
                      .event(AppConfigEvent.builder().build())
                      .security(AppConfigSecurity.builder().build())
                      .visibility(AppConfigVisibility.builder().build())
                      .contacts(AppConfigContactsRange.builder().build())
                      .event_and_callback_encrypt_strategy(EventAndCallbackEncryptStrategy.builder().build())
                      .callback(AppConfigCallback.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: PatchApplicationConfigResponse = client.application.v7.application_config.patch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.application.v7.application_config.patch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: PatchApplicationConfigRequest = PatchApplicationConfigRequest.builder() \
        .app_id("cli_a306c5476fb8d00c") \
        .department_id_type("open_department_id") \
        .user_id_type("open_id") \
        .request_body(PatchApplicationConfigRequestBody.builder()
                      .scope(AppConfigScope.builder().build())
                      .event(AppConfigEvent.builder().build())
                      .security(AppConfigSecurity.builder().build())
                      .visibility(AppConfigVisibility.builder().build())
                      .contacts(AppConfigContactsRange.builder().build())
                      .event_and_callback_encrypt_strategy(EventAndCallbackEncryptStrategy.builder().build())
                      .callback(AppConfigCallback.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: PatchApplicationConfigResponse = await client.application.v7.application_config.apatch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.application.v7.application_config.apatch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

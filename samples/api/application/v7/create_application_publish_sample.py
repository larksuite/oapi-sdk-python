# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.application.v7 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: CreateApplicationPublishRequest = CreateApplicationPublishRequest.builder() \
        .app_id("cli_a508dbf34038d01c") \
        .request_body(CreateApplicationPublishRequestBody.builder()
                      .mobile_default_ability("gadget")
                      .pc_default_ability("gadget")
                      .remark("更新了移动端默认应用能力")
                      .changelog("更新了小程序的头像")
                      .version("1.1.1")
                      .build()) \
        .build()

    # 发起请求
    response: CreateApplicationPublishResponse = client.application.v7.application_publish.create(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.application.v7.application_publish.create failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: CreateApplicationPublishRequest = CreateApplicationPublishRequest.builder() \
        .app_id("cli_a508dbf34038d01c") \
        .request_body(CreateApplicationPublishRequestBody.builder()
                      .mobile_default_ability("gadget")
                      .pc_default_ability("gadget")
                      .remark("更新了移动端默认应用能力")
                      .changelog("更新了小程序的头像")
                      .version("1.1.1")
                      .build()) \
        .build()

    # 发起请求
    response: CreateApplicationPublishResponse = await client.application.v7.application_publish.acreate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.application.v7.application_publish.acreate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

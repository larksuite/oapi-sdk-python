# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.task.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListRelatedTaskTaskV2Request = ListRelatedTaskTaskV2Request.builder() \
        .page_size(int) \
        .page_token("str") \
        .completed(bool) \
        .user_id_type("user_id") \
        .build()

    # 发起请求
    response: ListRelatedTaskTaskV2Response = client.task.v2.task_v2.list_related_task(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.task.v2.task_v2.list_related_task failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListRelatedTaskTaskV2Request = ListRelatedTaskTaskV2Request.builder() \
        .page_size(int) \
        .page_token("str") \
        .completed(bool) \
        .user_id_type("user_id") \
        .build()

    # 发起请求
    response: ListRelatedTaskTaskV2Response = await client.task.v2.task_v2.alist_related_task(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.task.v2.task_v2.alist_related_task failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.security_and_compliance.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateDeviceRecordRequest = UpdateDeviceRecordRequest.builder() \
        .device_record_id("7089353870308032531") \
        .version("0") \
        .user_id_type("user_id") \
        .request_body(DeviceRecord.builder()
                      .device_ownership(0)
                      .device_status(0)
                      .latest_user_id("ou_b25e90585ef8c1adac4b379c2e257906")
                      .dids([])
                      .is_managed(bool)
                      .mdm_device_id("123abc")
                      .mdm_provider_name("Workspace_ONE")
                      .build()) \
        .build()

    # 发起请求
    response: UpdateDeviceRecordResponse = client.security_and_compliance.v2.device_record.update(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.security_and_compliance.v2.device_record.update failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: UpdateDeviceRecordRequest = UpdateDeviceRecordRequest.builder() \
        .device_record_id("7089353870308032531") \
        .version("0") \
        .user_id_type("user_id") \
        .request_body(DeviceRecord.builder()
                      .device_ownership(0)
                      .device_status(0)
                      .latest_user_id("ou_b25e90585ef8c1adac4b379c2e257906")
                      .dids([])
                      .is_managed(bool)
                      .mdm_device_id("123abc")
                      .mdm_provider_name("Workspace_ONE")
                      .build()) \
        .build()

    # 发起请求
    response: UpdateDeviceRecordResponse = await client.security_and_compliance.v2.device_record.aupdate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.security_and_compliance.v2.device_record.aupdate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.security_and_compliance.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: GetDeviceRecordRequest = GetDeviceRecordRequest.builder() \
        .device_record_id("7089353870308032531") \
        .user_id_type("user_id") \
        .build()

    # 发起请求
    response: GetDeviceRecordResponse = client.security_and_compliance.v2.device_record.get(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.security_and_compliance.v2.device_record.get failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: GetDeviceRecordRequest = GetDeviceRecordRequest.builder() \
        .device_record_id("7089353870308032531") \
        .user_id_type("user_id") \
        .build()

    # 发起请求
    response: GetDeviceRecordResponse = await client.security_and_compliance.v2.device_record.aget(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.security_and_compliance.v2.device_record.aget failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.directory.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: FilterDepartmentRequest = FilterDepartmentRequest.builder() \
        .employee_id_type("open_id") \
        .department_id_type("open_department_id") \
        .request_body(FilterDepartmentRequestBody.builder()
                      .filter(MultiFilterCondition.builder().build())
                      .required_fields([])
                      .page_request(PageCondition.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: FilterDepartmentResponse = client.directory.v1.department.filter(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.directory.v1.department.filter failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: FilterDepartmentRequest = FilterDepartmentRequest.builder() \
        .employee_id_type("open_id") \
        .department_id_type("open_department_id") \
        .request_body(FilterDepartmentRequestBody.builder()
                      .filter(MultiFilterCondition.builder().build())
                      .required_fields([])
                      .page_request(PageCondition.builder().build())
                      .build()) \
        .build()

    # 发起请求
    response: FilterDepartmentResponse = await client.directory.v1.department.afilter(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.directory.v1.department.afilter failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.directory.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListCollborationShareEntityRequest = ListCollborationShareEntityRequest.builder() \
        .target_tenant_key("test_key") \
        .target_department_id("test_key") \
        .target_group_id("test_key") \
        .is_select_subject(bool) \
        .page_token("str") \
        .page_size(100) \
        .build()

    # 发起请求
    response: ListCollborationShareEntityResponse = client.directory.v1.collboration_share_entity.list(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.directory.v1.collboration_share_entity.list failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: ListCollborationShareEntityRequest = ListCollborationShareEntityRequest.builder() \
        .target_tenant_key("test_key") \
        .target_department_id("test_key") \
        .target_group_id("test_key") \
        .is_select_subject(bool) \
        .page_token("str") \
        .page_size(100) \
        .build()

    # 发起请求
    response: ListCollborationShareEntityResponse = await client.directory.v1.collboration_share_entity.alist(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.directory.v1.collboration_share_entity.alist failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

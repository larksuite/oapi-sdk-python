# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.corehr.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: TerminateSignatureFileRequest = TerminateSignatureFileRequest.builder() \
        .user_id_type("people_corehr_id") \
        .request_body(TerminateSignatureFileRequestBody.builder()
                      .ids([])
                      .operator("7386593105085988884")
                      .terminate_reason("需要重新签署, 因此终止签署的文件")
                      .build()) \
        .build()

    # 发起请求
    response: TerminateSignatureFileResponse = client.corehr.v2.signature_file.terminate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.corehr.v2.signature_file.terminate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: TerminateSignatureFileRequest = TerminateSignatureFileRequest.builder() \
        .user_id_type("people_corehr_id") \
        .request_body(TerminateSignatureFileRequestBody.builder()
                      .ids([])
                      .operator("7386593105085988884")
                      .terminate_reason("需要重新签署, 因此终止签署的文件")
                      .build()) \
        .build()

    # 发起请求
    response: TerminateSignatureFileResponse = await client.corehr.v2.signature_file.aterminate(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.corehr.v2.signature_file.aterminate failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.corehr.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: WorkCalendarLeaveRequest = WorkCalendarLeaveRequest.builder() \
        .request_body(WorkCalendarFilter.builder()
                      .wk_calendar_ids([])
                      .wk_calendar_id_gt("12344")
                      .wk_option(WkOption.builder().build())
                      .only_enable(True)
                      .build()) \
        .build()

    # 发起请求
    response: WorkCalendarLeaveResponse = client.corehr.v1.leave.work_calendar(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.corehr.v1.leave.work_calendar failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: WorkCalendarLeaveRequest = WorkCalendarLeaveRequest.builder() \
        .request_body(WorkCalendarFilter.builder()
                      .wk_calendar_ids([])
                      .wk_calendar_id_gt("12344")
                      .wk_option(WkOption.builder().build())
                      .only_enable(True)
                      .build()) \
        .build()

    # 发起请求
    response: WorkCalendarLeaveResponse = await client.corehr.v1.leave.awork_calendar(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.corehr.v1.leave.awork_calendar failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

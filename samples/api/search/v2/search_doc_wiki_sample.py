# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.search.v2 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SearchDocWikiRequest = SearchDocWikiRequest.builder() \
        .request_body(SearchDocWikiRequestBody.builder()
                      .query("飞书文档使用指南")
                      .doc_filter(DocFilter.builder().build())
                      .wiki_filter(WikiFilter.builder().build())
                      .page_token("token_1234567890fedcba")
                      .page_size(0)
                      .build()) \
        .build()

    # 发起请求
    response: SearchDocWikiResponse = client.search.v2.doc_wiki.search(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.search.v2.doc_wiki.search failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: SearchDocWikiRequest = SearchDocWikiRequest.builder() \
        .request_body(SearchDocWikiRequestBody.builder()
                      .query("飞书文档使用指南")
                      .doc_filter(DocFilter.builder().build())
                      .wiki_filter(WikiFilter.builder().build())
                      .page_token("token_1234567890fedcba")
                      .page_size(0)
                      .build()) \
        .build()

    # 发起请求
    response: SearchDocWikiResponse = await client.search.v2.doc_wiki.asearch(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.search.v2.doc_wiki.asearch failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()

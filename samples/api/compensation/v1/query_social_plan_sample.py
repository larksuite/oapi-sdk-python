# Code generated by Lark OpenAPI.

import lark_oapi as lark
from lark_oapi.api.compensation.v1 import *


def main():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: QuerySocialPlanRequest = QuerySocialPlanRequest.builder() \
        .request_body(QuerySocialPlanRequestBody.builder()
                      .plan_ids([])
                      .effective_date("2024-01-01")
                      .build()) \
        .build()

    # 发起请求
    response: QuerySocialPlanResponse = client.compensation.v1.social_plan.query(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.compensation.v1.social_plan.query failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


# 异步方式
async def amain():
    # 创建client
    client = lark.Client.builder() \
        .app_id(lark.APP_ID) \
        .app_secret(lark.APP_SECRET) \
        .log_level(lark.LogLevel.DEBUG) \
        .build()

    # 构造请求对象
    request: QuerySocialPlanRequest = QuerySocialPlanRequest.builder() \
        .request_body(QuerySocialPlanRequestBody.builder()
                      .plan_ids([])
                      .effective_date("2024-01-01")
                      .build()) \
        .build()

    # 发起请求
    response: QuerySocialPlanResponse = await client.compensation.v1.social_plan.aquery(request)

    # 处理失败返回
    if not response.success():
        lark.logger.error(
            f"client.compensation.v1.social_plan.aquery failed, code: {response.code}, msg: {response.msg}, log_id: {response.get_log_id()}")
        return

    # 处理业务结果
    lark.logger.info(lark.JSON.marshal(response.data, indent=4))


if __name__ == "__main__":
    # asyncio.run(amain()) 异步方式
    main()
